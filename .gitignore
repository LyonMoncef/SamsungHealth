# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# Database
*.db

# Android (projet Gradle à la racine depuis le virage on-device)
.gradle/
local.properties
.kotlin/
app/build/
app/release/
*.iml

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier

# Design handoff exports
*.zip

# Multi-agent runtime artifacts (briefs/results/traces per task-id) — never commit
work/
# Pentester POC scripts — fork-safe redundancy (already covered by work/)
**/poc/

# Code-as-vault logs (Phase B+, volatile — regénérés depuis JSONL sources)
docs/vault/logs/
# Obsidian local config (graph layout, hotkeys, plugins state) — pollue les diffs
docs/vault/.obsidian/

# Coverage map (Phase A.7) — regénéré par /sync-coverage, pas dans git
docs/vault/_index/coverage-map.json
coverage.json
.coverage*
.coveragerc-cartographer

# Artefacts de build & imports desktop — jamais dans le repo (archivés hors-ligne sur S2:/data3/Projets/SamsungHealth)
*.apk
desktop-import/
