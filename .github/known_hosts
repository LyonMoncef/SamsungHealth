# .github/known_hosts — fingerprints SSH VPS (D-2 anti-TOFU)
#
# À remplir POST-merge Phase 6, avant 1er deploy. Procédure :
#
# 1. Sur le poste qui a SSH access aux VPS :
#    ssh-keyscan -t ed25519 dev.samsunghealth.example >> known_hosts.tmp
#    ssh-keyscan -t ed25519 samsunghealth.example >> known_hosts.tmp
#
# 2. Vérifier que les fingerprints matchent ce que tu vois en SSH direct :
#    ssh-keygen -lf known_hosts.tmp
#
# 3. Commit les lignes concrètes ci-dessous (remplacer ce placeholder).
#
# Format :
# <hostname> ssh-ed25519 AAAAxxxxxx...
